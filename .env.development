# Local development: talk to the local NestJS backend (npm run start:dev on port 3000).
# Production builds ignore this file and fall back to the Railway URL in src/services/api.js.
VITE_API_BASE_URL=http://localhost:3000
