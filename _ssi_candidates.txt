1	dq|${it.productName}
2	dq|Back to Sales Invoices
1	dq|Custom
1	dq|Description
1	dq|e.g. Bank — Al Rajhi (current)
1	dq|Filter invoices
1	dq|Internal note for this return
1	dq|Internal notes (optional, printed on invoice)
1	dq|Invoice description (optional)
1	dq|Item (optional)…
1	dq|min(1350px, 98vw)
1	dq|min(440px, 96vw)
1	dq|Net
1	dq|Open item list
1	dq|Optional
4	dq|pi-field pi-full-width
1	dq|pi-footer-column pi-summary-column
1	dq|pi-lines-header pi-line-data-row
3	dq|pi-row-input-num pi-math-input
1	dq|pi-search-results pi-line-item-picker-results
1	dq|pi-summary-row pi-grand-total
1	dq|premium-table mgr-si-table-wrap
1	dq|Record payment
1	dq|Ref #
2	dq|Remove line
1	dq|Saving…
1	dq|Search affiliated or non-affiliated customer…
1	dq|Search product to add
1	dq|Search reference, customer, description…
1	dq|Search sales invoices
1	dq|Show item list
1	dq|table-cell mgr-si-cell-actions
1	dq|table-cell mgr-si-cell-amount
1	dq|table-cell mgr-si-cell-balance
1	dq|table-cell mgr-si-cell-customer
2	dq|table-cell mgr-si-cell-date
1	dq|table-cell mgr-si-cell-desc
1	dq|table-cell table-empty
1	dq|table-th mgr-si-th-actions
1	dq|View invoice
1	sq| pi-line-row-picker-open
1	sq|../../components/AutoGrowTextarea
1	sq|../../components/InlineFormScreen
1	sq|../../components/invoices/InvoiceRefField
1	sq|../../components/Modal
1	sq|../../components/RowActionsMenu
1	sq|../../components/supplier/Shimmer
1	sq|../../components/supplier/WorkshopPurchaseInvoiceView
1	sq|../../services/invoiceReferenceApi
1	sq|../../services/supplierApi
1	sq|../../styles/admin/AccountingPage.css
1	sq|../../utils/affiliatedCustomerLabels
1	sq|../../utils/invoiceLineLabel
1	sq|Account
1	sq|Add at least one line item.
1	sq|Add at least one line with an account or item name to save a draft.
2	sq|Affiliated Workshops
5	sq|ArrowDown
3	sq|ArrowUp
1	sq|Bank
1	sq|Bank — Al Rajhi
1	sq|Bank — SNB
1	sq|Bank transfer
4	sq|Box
1	sq|Brake Fluid DOT4
1	sq|Car Wash Normal - Small
1	sq|Card
2	sq|Cash
1	sq|Cheque
1	sq|Confirm paid
1	sq|Could not download invoice PDF.
1	sq|Could not initialize invoice PDF.
1	sq|Could not load invoice for return.
1	sq|Could not load invoice.
1	sq|Could not load workshop branches. Check that the app points at your backend (see api.js BASE_URL) and you are logged in as a supplier user.
1	sq|Could not record payment.
1	sq|Could not save return.
1	sq|Could not update payment status.
2	sq|Create Sale Invoice
2	sq|Custom
2	sq|Customer
1	sq|Customers
1	sq|Delete failed.
1	sq|Delete invoice failed:
1	sq|Download invoice failed:
1	sq|Download PDF
2	sq|Draft
2	sq|Edit
1	sq|Edit draft
1	sq|Engine Oil — Full Synthetic 5W40
5	sq|Enter
3	sq|Escape
2	sq|Exempt
1	sq|Failed to load invoice.
2	sq|Failed to load invoices.
1	sq|Failed to load.
1	sq|Failed to refresh invoices.
1	sq|Failed to save invoice.
1	sq|Inventory Asset
1	sq|Invoice
1	sq|Invoice #
1	sq|Invoice discount (fixed SAR):
2	sq|Invoice not found.
2	sq|Issue Sales Invoice
2	sq|List supplier invoices failed:
1	sq|Load invoice for edit failed:
1	sq|Loading invoice…
1	sq|Loading warehouse catalog and branches…
1	sq|Main Cash
1	sq|minmax(140px, 1.35fr)
2	sq|minmax(140px,2fr) 88px 104px 96px 112px minmax(120px,1fr)
7	sq|Net
1	sq|New
1	sq|No customers loaded. Add affiliated workshops or non-affiliated customers first.
1	sq|No invoices match your search or filter
1	sq|No matching customers.
1	sq|No matching products.
1	sq|No matching products. Try SKU or more of the product name.
1	sq|No matching products. Try SKU, more letters, or check the product is active in your catalog.
1	sq|No products available.
1	sq|No products loaded.
1	sq|No products loaded. Try again later or use “Add line” and type manually.
1	sq|No sales invoices yet
1	sq|No warehouse stock — you can still sell; you will be asked to confirm before issuing.
1	sq|Oil Filter — Universal
1	sq|Only invoices with status pending_payment (no payments) can be deleted.
1	sq|Other
1	sq|Overdue
2	sq|Paid
1	sq|Paid in full
1	sq|Partial
1	sq|Partially paid
1	sq|Pending payment
8	sq|Product
1	sq|Quantity
1	sq|Recording…
1	sq|Ref # (Optional)
1	sq|Sales invoice last-sale refresh failed:
1	sq|Sales Revenue
1	sq|Save as Draft
1	sq|Save supplier invoice failed:
2	sq|Saving…
1	sq|Select a customer.
1	sq|Select a payment method.
1	sq|Select receiving account or enter a custom account name.
1	sq|Selected customer is inactive.
1	sq|Service
1	sq|Stock sales price
1	sq|Submit return
1	sq|Supplier catalog search failed:
1	sq|Supplier customer-branches failed:
1	sq|Supplier sales invoices bootstrap failed:
2	sq|Tab
1	sq|Unpaid
1	sq|Update Invoice
1	sq|Update payment status failed:
2	sq|VAT 0%
7	sq|VAT 15%
2	sq|VAT 5%
1	sq|View
1	sq|View invoice failed:
1	sq|WPI-SI
1	tl| · ${invoiceRangeStart}–${invoiceRangeEnd} of ${invoiceListTotal}
1	tl|• ${item.unit}
1	tl|• ${row.name}: ${row.requestedQty} ${row.unit} (available: ${row.availableQty} ${row.unit})
1	tl|${buyerWorkshop} — ${buyerBranch}
1	tl|${Date.now()}-${Math.random().toString(36).slice(2, 9)}
1	tl|${inv.workshopName} — ${inv.branch}
1	tl|${item.id}-${item.name}
1	tl|${line.id}-${String(invItem.id)}-${i}
1	tl|${line.id}:account
1	tl|${line.id}:description
1	tl|${line.id}:discount
1	tl|${line.id}:discountMode
1	tl|${line.id}:item
1	tl|${line.id}:price
1	tl|${line.id}:qty
1	tl|${line.id}:taxCode
2	tl|${line.id}:uom
1	tl|${maxQtyCap} ${line.uom || 'pcs'} available — supplier stock
1	tl|${nameBase} (${typeLabel})
1	tl|${opt.code} - ${opt.name}
1	tl|${prefix}-${Date.now().toString(36).toUpperCase()}
1	tl|${qty} ${wsu} = ${whQty} ${wu} warehouse · SAR ${price.toFixed(2)}/${wsu} → SAR ${whPrice.toFixed(2)}/${wu}
1	tl|${qty} ${wu} = ${wsQty} ${wsu} at workshop · SAR ${price.toFixed(2)}/${wu} → SAR ${wsPrice.toFixed(2)}/${wsu}
1	tl|${returnModalRow.workshopName} — ${returnModalRow.branch}
1	tl|Actions for invoice ${inv.invoiceNo || inv.id}
1	tl|affiliated:workshop:${String(m.affiliatedWorkshopId)}
1	tl|branch|${String(inv.branchId).trim()}
1	tl|Delete invoice ${row.invoiceNo}?
1	tl|Exceeds stock (${maxQtyCap} ${line.uom || 'pcs'} available) — confirm on save
1	tl|external:${String(m.externalPartyId)}
1	tl|GET /supplier/cash-bank/accounts
1	tl|Invalid qty for ${it.productName || 'line'}.
1	tl|Invoice discount (${invPctDisplayed}%):
1	tl|Line ${invalidLine.index + 1}: select an account (or enter item name), qty must be > 0, and price cannot be negative.
2	tl|Net ${netDays}
1	tl|Payment received for sales invoice ${invoiceNo}
1	tl|per ${item.unit}
1	tl|pi-due-grid ${
                                            dueDateType === 'EOM' ? 'pi-due-eom' : ''
                                        }
1	tl|pi-lines-header pi-line-data-row${
                                            itemPickerLineId === line.id && itemPickerMenuOpen
                                                ? ' pi-line-row-picker-open'
                                                : ''
                                        }
1	tl|pi-result-item ${
                                                                                itemPickerSelectedIndex ===
                                                                                i
                                                                                    ? 'selected'
                                                                                    : ''
                                                                            }
1	tl|pi-result-item ${
                                                                            customerPickerIndex ===
                                                                            index
                                                                                ? 'selected'
                                                                                : ''
                                                                        }
1	tl|pi-result-item ${
                                                                selectedIndex === index
                                                                    ? 'selected'
                                                                    : ''
                                                            }
1	tl|Quantity. Available ${maxQtyCap} ${line.uom || 'pcs'} (supplier stock balance).
1	tl|row-${item.productName}-${item.sku || ''}
1	tl|SAR ${Number(
                                                            returnInvoiceDetail.invoice.grandTotal ?? 0,
                                                        ).toLocaleString(undefined, {
                                                            minimumFractionDigits: 2,
                                                            maximumFractionDigits: 2,
                                                        })}
1	tl|SAR ${Number(
                                                            returnInvoiceDetail.invoice.outstanding ?? 0,
                                                        ).toLocaleString(undefined, {
                                                            minimumFractionDigits: 2,
                                                            maximumFractionDigits: 2,
                                                        })}
1	tl|SAR ${Number(
                                                            returnInvoiceDetail.invoice.paid ?? 0,
                                                        ).toLocaleString(undefined, {
                                                            minimumFractionDigits: 2,
                                                            maximumFractionDigits: 2,
                                                        })}
1	tl|SAR ${Number(
                                                            returnInvoiceDetail.invoice.returnsTotal ?? 0,
                                                        ).toLocaleString(undefined, {
                                                            minimumFractionDigits: 2,
                                                            maximumFractionDigits: 2,
                                                        })}
1	tl|Stock sales price SAR ${roundMoney2(catalogSalePrice * conversionFactor).toFixed(2)} / ${warehouseUnit} incl. VAT
1	tl|Suggested list SAR ${suggestedWh.toFixed(2)} / ${warehouseUnit} (invoice default)
1	tl|The following product(s) exceed available supplier stock:\n\n${detail}\n\nContinue anyway? Your stock inventory timeline will show a negative balance for these items.
1	tl|Warehouse stock: ${qtyWh} ${warehouseUnit} • Unit cost SAR ${unitCostWh.toLocaleString(undefined, { maximumFractionDigits: 4 })} / ${warehouseUnit}
1	tl|Workshop order ${prefill.workshopPurchaseInvoiceNumber}
1	tl|workshop|${String(inv.workshopId).trim()}
1	tl|WorkshopPurchaseInvoiceView